<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand">Микроклимата</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Мониторинг
            </NavLink>
        </div>     
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="predictionsjournal">
                <span class="oi oi-list" aria-hidden="true"></span> Журнал прогнозов
            </NavLink>
        </div>     
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="predictionsgraphics">
                <span class="oi oi-graph" aria-hidden="true"></span> Графики прогнозов
            </NavLink>
        </div>
@*        <div class="nav-item px-3">
            <NavLink class="nav-link" href="predictionsgraphics">
                <span class="oi oi-reload" aria-hidden="true"></span> Настройка
            </NavLink>
        </div>*@
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
